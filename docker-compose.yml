services:
  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped

# Si ORDS también corre en Docker, descomenta y ajusta:
#  ords:
#    image: container-registry.oracle.com/database/ords:latest
#    ports:
#      - "8080:8080"
#    environment:
#      CONN_STRING: <usuario>/<password>@<host>:<puerto>/<servicio>
#    restart: unless-stopped
